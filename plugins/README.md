# `@aoe/plugins`

### 🔨 使用 unbuild 进行构建

### 🪜 其它问题

> **dirname is not defined in ES module scope
> 在 ES 模块中，`**dirname`不可用，可以使用`import.meta.url` 代替。
